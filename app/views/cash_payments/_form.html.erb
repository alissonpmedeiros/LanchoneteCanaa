<%= form_for(@cash_payment) do |f| %>
  <% if @cash_payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cash_payment.errors.count, "error") %> prohibited this account from being saved:</h2>

      <ul>
      <% @account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="container"> 
    <div class="row">
      <form class="col s12">
        <div class="row">
          <div class="col s12 m4 l2"><p></p></div>
          <div class="input-field col s12 m4 l8">
            <i class="material-icons prefix">payment</i>
            <%= f.text_field :price %>
            <% if params[:action] == 'new' %>
              <label for="icon_prefix"><%= :price %></label>
            <% end %>  
          </div>
          <div class="col s12 m4 l2"><p></p></div>
        </div>

        <div class="row">
          <div class="col s12 m4 l2"><p></p></div>
          <div class="input-field col s12 m4 l8">
            <i class="material-icons prefix">supervisor_account</i>
            <%= f.text_field :salesman %>
            <% if params[:action] == 'new' %>
              <label for="icon_prefix"><%= :salesman %></label>
            <% end %>  
          </div>
          <div class="col s12 m4 l2"><p></p></div>
        </div>

        <div class="row">
          <div class="col s12 m4 l2"><p></p></div>
          <div class="input-field col s12 m4 l8">
            <i class="material-icons prefix">label_outline</i>
            <%= f.text_area :description %>
            <% if params[:action] == 'new' %>
              <label for="icon_prefix"><%= :description %></label>
            <% end %>  
          </div>
          <div class="col s12 m4 l2"><p></p></div>
        </div>        
        <div class="col s12 m4 l2"><p></p></div>
        <div class="col s12 m4 l8">
          <button class="btn waves-effect waves-light" type="submit" name="action">
            Submit
          </button>
        </div>
        <div class="col s12 m4 l2"><p></p></div>
       
      </form>
    </div>
  </div>

<% end %>
